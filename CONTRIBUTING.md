# 함께 만들기

시수핏은 선생님들이 쓰는 도구입니다. 코드를 몰라도 도울 수 있습니다.

## 코드 없이 돕기

- **일정 이름 사례 알려 주기**: 우리 학교 학사일정에서 자동 분류가 틀린 일정 이름과 원래 뜻을 [이슈](../../issues)로 남겨 주세요. 예: "`창체3-4(진로)`는 3~4교시 진로활동입니다."
- **시간표 형식 알려 주기**: 읽지 못하는 시간표 파일이 있으면, 교사 이름을 지운 파일을 이슈에 올려 주세요.
- **계산이 이상할 때**: 어느 반·과목 숫자가 왜 틀렸다고 보는지 알려 주세요. 화면에서 칸을 누르면 나오는 "빠진 교시 근거"를 함께 적어 주시면 빨리 고칠 수 있습니다.

> 이슈에 학생 이름이나 교사 실명을 올리지 마세요. 교사 이름은 "국어1"처럼 바꿔 주세요.

## 코드로 돕기

```bash
cd web
npm install
npm test
npm run dev
```

- 계산 규칙은 `web/src/engine/`에 있고 모두 테스트가 있습니다. 규칙을 바꾸면 `engine.test.ts`에 실제 사례를 테스트로 더해 주세요.
- 새 일정 표기를 지원할 때는 `parseEvents.ts`의 `DEFAULT_RULES` 낱말 목록이나 교시 표기 정규식을 고치고, `실제 표기` 테스트 표에 한 줄을 더합니다.
- 새 시간표 형식은 `importTimetable.ts`에 배치(layout)를 더하고, 익명화한 작은 예시로 테스트합니다.
- 화면 글은 쉬운 우리말로 씁니다. 사용자가 누를 버튼은 무엇이 일어나는지 그대로 적습니다 ("설정 파일 저장").
- 실제 학교 자료(실명 시간표, 학교 캘린더)는 저장소에 올리지 않습니다. 필요하면 `web/data-private/`(git 제외)에 두세요.
